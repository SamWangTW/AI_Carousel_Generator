# Pipeline package.
# Each module in this package represents one stage of the AI carousel generation pipeline.
# Stages run sequentially: transcript → planner → slide_writer → validator → caption_writer → renderer
